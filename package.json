        {
  "name": "ipd-long-translate",
  "version": "1.0.0",
  "description": "长句翻译（由 tran Vue 项目迁移的纯静态页，无构建）",
  "private": true,
  "main": "index.html",
  "scripts": {
    "start": "npx --yes http-server . -c-1 -p 8000",
    "dev": "npx --yes live-server --port=8000 --host=localhost",
    "server": "npx --yes http-server . -p 8000 -c-1"
  }
}
